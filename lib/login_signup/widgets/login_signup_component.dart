import 'package:flutter/material.dart';

class LoginSignupComponent extends StatelessWidget {
  const LoginSignupComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}