import 'package:flutter/material.dart';

class FriendsComponent extends StatelessWidget {
  const FriendsComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}