import 'package:flutter/material.dart';

class CoursePlannerScreen extends StatelessWidget {
  const CoursePlannerScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('course_planner_screen'),
      ),
    );
  }
}