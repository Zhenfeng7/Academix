import 'package:flutter/material.dart';

class CoursePlannerComponent extends StatelessWidget {
  const CoursePlannerComponent({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}